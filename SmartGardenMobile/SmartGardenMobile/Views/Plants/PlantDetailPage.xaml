<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SmartGardenMobile.Localization;assembly=SmartGardenMobile"
             x:Class="SmartGardenMobile.Views.Plants.PlantDetailPage"
             Title="{Binding Title}">
    
    <StackLayout Spacing="20" Padding="15">
        <Label Text="{local:Translate Name}" FontSize="Medium" />
        <Label Text="{Binding Name}" FontSize="Small"/>
        <Label Text="{local:Translate Description}" FontSize="Medium" />
        <Label Text="{Binding Description}" FontSize="Small"/>
        <Label Text="{local:Translate Location}" FontSize="Medium" />
        <Label Text="{Binding Location}" FontSize="Small"/>
    </StackLayout>
</ContentPage>