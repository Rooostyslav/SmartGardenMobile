<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SmartGardenMobile.Localization;assembly=SmartGardenMobile"
             x:Class="SmartGardenMobile.Views.UserCabinetPage"
             Title="{local:Translate UserCabinet}">

    <StackLayout Spacing="20" Padding="15">
        <Label Text="{local:Translate UserName}" FontSize="Medium" />
        <Label Text="{Binding Name}" FontSize="Small"/>
        <Label Text="{local:Translate Email}" FontSize="Medium" />
        <Label Text="{Binding Email}" FontSize="Small"/>
        <Button VerticalOptions="Center" Text="{local:Translate GardensList}" Command="{Binding MoveToGardensListCommand}"/>
    </StackLayout>

</ContentPage>